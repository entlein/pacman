apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    # checkout the tutorial here https://kubernetes.github.io/ingress-nginx/examples/auth/basic/
    # type of authentication
    nginx.ingress.kubernetes.io/auth-type: basic
    # name of the secret that contains the user/password definitions
    nginx.ingress.kubernetes.io/auth-secret: basic-auth
    # message to display with an appropriate context why the authentication is required
    nginx.ingress.kubernetes.io/auth-realm: 'Authentication Required - foo'
  name: ingress-$pacman
  namespace: $pacman
spec:
  ingressClassName: nginx
  rules:
  - host: ingress-$pacman.k8s.dev.austrianopensciencecloud.org
    http:
      paths:
      - backend:
          service:
            name: pacman
            port:
              number: 80
        path: /
        pathType: Prefix
  #tls:
  #- hosts:
  #  - ingress-$pacman.k8s.dev.austrianopensciencecloud.org
  #  secretName: $pacman-ingress-tls